

<ul>
  <% Refinery::Blog::Category.where(parent_id: nil).each do |category| %>
    <%= render partial: 'category_tree_on_category_home', locals: { category: category } %>
  <% end %>
</ul>

<%= render "/refinery/admin/sortable_list",
           :continue_reordering => (defined?(continue_reordering) ? continue_reordering : true) %>
