<ul id='sortable_list'>
  <% categories = Refinery::Blog::Category.where(parent_id: nil) %>
  <%= render partial: 'category_tree_for_index', locals: { categories: categories } %>
</ul>
<%= render "/refinery/admin/sortable_list",
           :continue_reordering => (defined?(continue_reordering) ? continue_reordering : true) %>
